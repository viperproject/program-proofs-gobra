package chapter01

func Triple_v4(x int) (r int) {
	if x == 0 {
		r = 0
	} else {
		y := 2 * x
		r = x + y
	}
	assert r == 3 * x
}

// Unlike Dafny, Gobra does not yet support any kind of
// non-deterministic control flow.
